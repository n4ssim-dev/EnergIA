HOST=0.0.0.0
PORT=8007
API_PASSWORD=5

# URL du microservice prédictif. En local (uv run, hors docker), utiliser
# http://localhost:8005 (port hôte exposé par ms-predictive). Quand
# ms_metier tourne via `docker compose up` dans le même réseau que
# ms-predictive, utiliser le nom du service Docker :
PREDICTIVE_URL=http://ms-predictive:8005