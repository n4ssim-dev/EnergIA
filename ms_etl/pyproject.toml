[project]
name = "ms-etl"
version = "0.1.0"
description = "Service FastAPI EnergIA — ETL"
readme = "README.md"
authors = [
    { name = "Nassim", email = "achnassim34@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
