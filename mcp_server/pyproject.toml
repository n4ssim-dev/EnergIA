[project]
name = "mcp-server"
version = "0.1.0"
description = "Service MCP FastAPI EnergIA "
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "haversine>=2.9.0",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
