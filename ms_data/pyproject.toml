[project]
name = "ms-data"
version = "0.1.0"
description = "Service FastAPI EnergIA — ETL + Postgres (relationnal.db mirroré vers ms-data-postgres)"
readme = "README.md"
authors = [
    { name = "Nassim", email = "achnassim34@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
