[project]
name = "fastapi-service"
version = "0.1.0"
description = "Service FastAPI EnergIA — fusion de dijkstra/ et python-service/"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "haversine>=2.9.0",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
